version: "3.9"

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  api:
    build: ./backend
    ports:
      - "3000:3000"
    depends_on:
      - postgres